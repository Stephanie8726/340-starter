<h1>Login</h1>

<form method="POST" action="/account/login" class="account">
  <p>Enter your Email and Password</p>
  <label for="email">*Email:</label>
  <input type="email" id="email" name="account_email" required>

  <label for="password">*Password:</label>
  <input type="password" name="account_password" id="password" required
    pattern="^(?=.*\d)(?=.*[a-z])(?=.*[A-Z])(?=.*[^a-zA-Z0-9])(?!.*\s).{12,}$">

  <div>
    <i>Passwords must be at least 12 characters and contain 1 number, 1 capital letter, and 1 special
      character.</i>
  </div>
  <button type="submit">Login</button>
</form>

<p>No account? <a href="/account/register">Sign-up</a>.</p>

